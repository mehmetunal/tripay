<Project>
  <!--
    TriPay.Tests derlendikten sonra tüm testleri çalıştırır; başarısız test build'i düşürür.
    Solution: dotnet build TriPay.sln
    Kapatmak için: dotnet build -p:RunTestsOnBuild=false
  -->
  <PropertyGroup>
    <RunTestsOnBuild Condition="'$(RunTestsOnBuild)' == ''">true</RunTestsOnBuild>
  </PropertyGroup>

  <Target Name="RunTriPayTestsAfterBuild"
          AfterTargets="Build"
          Condition="'$(IsTestProject)' == 'true'
                     AND '$(RunTestsOnBuild)' == 'true'
                     AND '$(DesignTimeBuild)' != 'true'">
    <Message Importance="high" Text="TriPay: $(MSBuildProjectName) — testler çalıştırılıyor (başarısız test = build hatası)..." />
    <Exec Command="dotnet test &quot;$(MSBuildProjectFullPath)&quot; --no-build --verbosity minimal"
          WorkingDirectory="$(MSBuildProjectDirectory)" />
  </Target>
</Project>
